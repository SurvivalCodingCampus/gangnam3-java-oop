배운점->
    메인 프로젝트->
        거의 모든 것은 객체다.현재까지 앞에 생성할떄 new 가 있으면 그렇게 판단하면 될 것 같다.
        constructor 을 만들떄
        constructor nesting은 기본적으로 안되나 this(p1,p2...)를 맨 상단에 위치시키면 가능하다
        {}라는 기본 constructor 옵션도 있다 그냥 모든 constructor 할떄 실행되는 것이다.
        constructor 형식은 대응되는 것이 있어야지 실행이 된다 예를 들어서 호출될떄 매개변수와 클래스 내부에서 선언되는 매개변수 형식이 일치해야한다.
        물론 모든 constructor에서 특정 명령어들을 실행하고 싶으면 외부 함수를 선언하고 그 함수를 constructor안에서 작동하게 하는것이 좋아보인다.
        AI TEST코드가 오류를 잘 잡아준다 오류가 없을 줄 알았는데 생각보다 많이 있었다
            1.constructor 안에서 새로운것 선언할떄 hp=999 하면 최대 hp보다 많으나 그걸 체크하는걸 만들지 않음
            2.예외 사항일떄 return 값을 고정 할 필요가 있어보임 67로 했으나 0 ,-1등으로 하는것이 좋아 보임
            3.return Max_MP-MP를 했는데 MP라는 값을 중간에 바꿀수도 있다.그래서 바꾸기 전에 MP값을 다른것에 저장하는것이 좋아 보임.return 값을 MP가 바뀐 값을 써서 오류가 남.
        static있냐 없냐에 따라서 공유하는 변수냐 아니냐라고 간단하게 생각할수 있다
        static int x 라고 constructer에 쓰면 어떤 객체에서 x값이 1 상승하면 모든 객체에서 1만큼 상승한다.
        그냥 int라 하면 그 논리가 성립하지 않는다.
        public/private 은 뒤에 나온것을 어디서 접근이 가능한지 제한하는 기능을 한다.




    서브 프로젝트->
        별 다양한 객체들이 나온다 이상한 이름이 있으면 어떤 객체의 타입을 선언하는 것일수도 있다
        이벤트라는 것이 존재한다 키보드 마우스같은것을 이렇게 처리하는듯하다.
        파일->랜더링 파일 ->화면 이런 삼중구조가 자주 발견된다.어떤 api 시스템을 활용할려면 직접적으로 활용하는것은 어려운가 보다. git 저장구도 working directory->staging space->repository 와 유사한 구조인가 보다.
        x,y 평면은 왼쪽 상단을 0,0 으로 하고 특이하게 밑쪽으로 갈수록 y 값이 증가하는 메커니즘을 사용한다.
        JFrame 을 만들고 거기에 JLabel 를 추가적인 태그를 달고 그것을 매개변수로 받는 시스템이 존재한다.
        배열 리스트 노드.. 등의 자료 구조가 존재한다.
        통신에서 패킷이라는 것이 존재한다.
        함수 명령어는 접근 권한이나 루프 위치 따라 접근 가능성이 제한되는 시스템이 존재한다.

느낀점->
    각 라이버리당 각 명령어 집합들이 있다.이를 빠르게 활용할려면 새로운것을 빠르게 익숙해지는것에 대한 필요성이 느껴졌다.
    2시간 반동안 10줄을 빈칸에 쓸 수 있다는 암기력의 한계에서 약간 회의감을 느껴서 전략을 바꿔야 하겠다는 생각을 하였다.
    매일마다 공부 전략이 바뀌는것이 재미있게 느껴진다.
    
