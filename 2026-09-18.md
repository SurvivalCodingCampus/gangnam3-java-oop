배운점->
abstract interface 도 있음
list 기초 한번 봄
예습하면 좋을것으로 예상됨
static int 공유재
static fun
static class 그냥 위에 놈 instance 없어도 편하게 쓰게함.
main(int x){
    main(x);
}하면 무한 루프 물론 저세상 우회법이 있는것 같지만 아직 모르겠다.
get 이름 set 이름 private 변수 접근용
    메인 프로젝트->
    그림에서 표현
    느낀점->💀💀💀

    //상속은 원래 있던것을 확장하는 것이다.즉 자식의 것을 가져가는 것이다.
부모>>아이 A->B 라 할떄 A 가 상속 받는 놈이라고 해석하는 놈이 있다.
//과정과 결과를 생각해야 할 떄도 있다.
//부모가 있어야지 자식이 생성 가능하다 이 논리로 부모(){
    super 자식으로 해야지 정상작동한다
}    
그냥 
부모(){
    this.x=x;
    this.y;
    //기본 생성자는 자동으로 호출 된다 그래서 자식에서 기본 생성자가 있을떄 this.x this.y라 해도 된다.ㄹ
}