# 인스턴스와 클래스

## 용어 정리
## 내가 이해한 오늘의 수업
- 메소드 안에 있는 객체들은 호출시 시간순서의 흐름으로 진행된다.
- 리턴 값이 필요한 수식에는 int를 써야한다. 
- 외부 값을 숫자로 나가면 int 같은 애들 
- 외부 값을 글자로 나갈려면 String 같은거다.
- 메소드는 하나의 상자같은거다. 주의
- 메소드 안에서 발생하는 결과값을 주기적으로 갱신해야한다.

int pray(int sec) {
        int oldmp = this.mp;
        Random random = new Random();
        int bonus = random.nextInt(3);
        int prayTime = sec + bonus;
        int prayDo = prayTime + mp;
        if (prayDo > MAXMP) {
            prayDo = MAXMP;
            System.out.println("이미 마나가 최대입니다.");
        }
        this.mp = prayDo;
        return prayDo - oldmp;

- 논리구조
    - 기존에 있던 마나를 스킬 사용시 마나가 채워지는 시스템 안에서는 기존의 마나와 스킬 사용시 채워지는 마나가 분리되어야 한다.
    - 현재의 마나가 얼마인지에 대한 이정표가 필요하다.
    - 램덤한 값과 시간을 더한 값이 최대 마나 보다 크다면 최대 마나와 동일하게 출력되어도 충분하다.
    - 앞서 현재의 마나와 지금의 마나의 값을 빼야지 내가 더해진 마나의 값을 알 수 있다. this.mp = prayDo;
        return prayDo - oldmp;

Random random = new Random();
        int bonus = random.nextInt(3);
        
        이건 3되기 전까지의 0 1 2 이렇게 숫자를 넣는다라는 뜻

- int oldmp = this.mp; 를 사용한 이유
    - 메소드를 호출할때의 사용자의 마나값은 다 다르다. 그렇기에 this.mp;를 사용한 거다. 동시에 지금의 마나를 저장을 해야지만 더해진 마나의 값을 측정할 수 있다.

- for 는 내가 숫자 정해서 돌리기
- while 은 특정 조건 달성할때까지 돌리기 
- void 빼고는 다 리턴 값이 있음 , 리턴 값이 무조건 나와야함.
- 메소드의 호출 시간 중요, 또 중요.. 메소드는 호출 시에 시간으로 초기값이 결정되는거 같다.
- 엡실론은 아주 작은 오차 범위를 설정하기 위해서 만든거다.  일반 컴퓨터는 0.2 + 0.3 같은거가 불가능하다.

- math.ulp()이거 하면 소수점 날리고 함. 

- 마스터 브랜치에서 작업하기 좀 힘들거 같을때는 브랜치를 만들면 좋을거 같다. 브랜치는 평행우주, 가지 같은거다.
너무 많은건 관리 힘들거다. 

- 보일러 플레이트 = 맨날하는 "거를" 그렇게 말한다. 

- 버그가 나는 지점을 명확하게 찾으려면 명확한 지점을 찾아서 해야한다. 

- 자바스크립는 자바와 관계가 없다. 서로 다른 프로그래밍 언어라고 보면 된다.

- 클래스 = 캐릭터 설계도, 건축 도면
- 인스턴스 = 설계도를 기반으로 만든 캐릭터, 준공 건물
- 오브젝트 = 캐릭터 자체 , 부동산 // 인스턴스랑 겹치는 부분이 좀 있음
- 필드 = 캐릭터 스텟, 집의 요소들 엘리베이터, 환풍기..
- 매서드 = 캐릭터의 행동, 집에서 대전차포를 작동할 수 있는것인지.. 

-함수와 메서드의 차이.
    함수는 인풋이 있으면 아웃풋이 동일하다. 그런데 메서드는 임마는 함수처럼 생겼는데, 어떤 클래스 안에 기능을 표현하는거임, 객체의 동작을 표현하는거임, 그러면 이건 함수로 볼 수 없음. 이건 외부 요인들이 있다. 항상 동일하게 하는 값이 안나옴. 생긴건 함수지만 밖에 있는 애들이 관여를 하니까.. 슬립을 하면 hp는 100인데 이름은 항상 뭐 홍길동을 필요는 없다. 즉 객체의 기능을 표현하는건 메서드다.











