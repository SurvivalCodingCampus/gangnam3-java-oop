# [TIL] Day 05 - 클래스

- **작성자**: 엄시형 (SiHyeong Eom)
- **작성일**: 2026-09-17

---

## 1. 배운 점 (미완성)

- `파일 삭제나 깃 잘못 사용 사용시` : 인텔리제이 히스토리를 통해 복구 가능
- `@RepeatedTest(int)` : 특정 횟수만큼 반복 가능
- `@BeforeEach`: 테스트 전 해당 함수를 실행
- `Java가 등장한 이유` : 기존에 C언어는 운영체제에 맞는 컴일러를 통해 컴파일러의 의존적이였음
  자바는 JVM이라는 
- `JVM` : 
- `테스트 코드 즁요성` : 
- `레퍼런스` : 
- `Heap` : 동적할당
- `Stack` : 지역변수

- `static` : 
- `유닛테스트` : 
- `테스트 장점` :
- `경계값 분석(추천)` :
- `화이트박스` : 내부 구조와 동작 테스트
- `블랙박스` : 내부 동작은 무시하고 기능만 테스트 
-  `생성자 안에서 생성자 호출` : 
```java
ctor() {
	this(...)
}
```
---

## 2. 가장 멘붕이었던 순간 & 트러블슈팅



